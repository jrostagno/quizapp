# Application
LOG_LEVEL=INFO

# Database (dev)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/quizapp

# Database (test) — separate container
TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5433/quizapp_test

# Redis (arq broker)
REDIS_URL=redis://localhost:6379

# MCP — GitHub (obtainable via `gh auth token`)
GITHUB_TOKEN=

# MCP — Context7 (https://context7.com/)
CONTEXT7_API_KEY=
